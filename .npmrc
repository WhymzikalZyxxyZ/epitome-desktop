# Native modules (better-sqlite3) are compiled for system Node here.
# electron-builder rebuilds them for Electron automatically during `npm run package`.
# After packaging, run `npm run rebuild:node` to restore the dev build.
